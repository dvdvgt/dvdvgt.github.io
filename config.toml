# theme = "apollo"
# taxonomies = [{ name = "tags" }]

# website's header title
title = "David Voigt"
author = "David Voigt"

# The URL the site will be built for
base_url = "https://dvoigt.de"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
katex = true

[extra.footer]
show_copyright = true
socials = [
  { name = "GitHub", url = "https://github.com/dvdvgt", path = "github.svg" },
  { name = "Website", url = "https://se.informatik.uni-tuebingen.de/team/voigt/", path = "website.svg" },
  { name = "mail", url = "mailto:website@dvoigt.de", path = "mail.svg" }
]

[extra.nav]
links = [
    { name = "home", url = "/", weight = 1 },
    { name = "posts", url = "/posts", weight = 1 },
]
